<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
 android:layout_height="wrap_content" 
  android:layout_width="fill_parent">
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:id="@+id/dialog_layout_root"
              android:orientation="vertical"
              android:layout_width="fill_parent"
              android:layout_height="wrap_content"
              android:padding="10dp">
 
 <TextView
    android:id="@+id/selectTransportTitle"
    android:text="Select Transport:"
  android:layout_height="wrap_content"
  android:layout_width="fill_parent" 
  android:textSize="14sp"
  android:textStyle="bold"/>
<TextView
    android:id="@+id/selectTrnsportText"
  android:layout_height="wrap_content"
  android:layout_width="fill_parent" 
  android:textSize="14sp"/>

    <Spinner
        android:id="@+id/spinner3"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/transport_arrays"
        android:prompt="@string/transport_prompt" />
    
    <Button
                 android:id="@+id/getRating_button"
                 android:text="Get Rating" 
                 android:padding="10dip"
                 android:background="@drawable/red_btn" style="@style/ButtonText"/>
              </LinearLayout>
              </ScrollView>